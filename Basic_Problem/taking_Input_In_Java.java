
package Basic_Problem;

import java.util.Scanner;

import java.util.*;

public class taking_Input_In_Java {
    public static void main(String args[]) {

        Scanner sc = new Scanner(System.in); // (sc ) is object name we can use (s) or (scn) or (any name)
        // String name = sc.next();
        // System.out.println(name);

        String name_2 = sc.nextLine();
        System.out.println(name_2);

        // types of input in java
        /*
         * next();
         * nextLine();
         * nextInt();
         * nextByte();
         * nextFloat();
         * nextDouble();
         * nextBoolean();
         * nextShort();
         * nextLong();
         * 
         */
    }
}
